services:
  md-converter:
    build: .
    container_name: md-converter
    ports:
      - "5000:5000"   # http://localhost:8080
    volumes:
      - ./:/app       # facilita editar arquivos localmente
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
